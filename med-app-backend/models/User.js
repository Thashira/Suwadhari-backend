// User Schema (Prisma or Sequelize)
// This is a placeholder for database schema definition

/*
Prisma Schema Example:
model User {
  id        Int     @id @default(autoincrement())
  email     String  @unique
  password  String
  name      String
  role      String  @default("user")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
*/
